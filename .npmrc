# Always install devDependencies, even when NODE_ENV=production (Railway).
# The production build needs build-only tooling (Tailwind/PostCSS, TypeScript,
# ESLint, the Prisma CLI). This is a build/runtime image, not a pruned artifact.
include=dev
production=false
